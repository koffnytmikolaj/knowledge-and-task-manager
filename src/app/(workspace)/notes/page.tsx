import WorkspaceSection from "shared/ui/workspace/WorkspaceSection";

export default function NotesPage() {
  return (
    <WorkspaceSection
      title="Notes"
      description="Capture and organize the knowledge that supports your work."
    />
  );
}