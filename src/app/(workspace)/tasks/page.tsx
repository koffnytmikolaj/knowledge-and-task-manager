import WorkspaceSection from "shared/ui/workspace/WorkspaceSection";

export default function TasksPage() {
  return (
    <WorkspaceSection
      title="Tasks"
      description="Turn knowledge into focused actions and keep work moving."
    />
  );
}